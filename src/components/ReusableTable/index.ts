// Entry point – import everything from here
export { Table } from './Table';
export type { TableProps, Column, SortDirection } from './Table';

// Mock data (for development / testing only)
export { mockUsers, mockProducts, mockOrders } from './mockData';
export type { User, Product, Order } from './mockData';
